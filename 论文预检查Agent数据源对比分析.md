# 论文预检查 Agent 数据源对比分析（maker vs mineru）

**项目：** fudan-pager-check  
**对比文件：**
- `基于集成深度学习模型的公路隧道交通流预测_钱超_maker.md`
- `基于集成深度学习模型的公路隧道交通流预测_钱超_mineru.md`

---

## 一、结论（优先阅读）

**推荐优先使用 `*_maker.md` 作为主数据源**；`*_mineru.md` 可作为表格/图表结构的补充，**不宜单独用于预检查**。

若只能选一个：**选择 maker.md**。

---

## 二、对比概览

| 维度 | maker.md | mineru.md |
|------|----------|-----------|
| 体量 | 436 行 / ~39KB | 1165 行 / ~58KB |
| 正文完整性 | 高，关键数值、术语基本保留 | 低，大量空白/缺失 |
| 章节结构 | 有层级编号（1、2.1、3.3.1 等） | 编号丢失，标题扁平 |
| 表格 | Markdown 表格，数据完整 | HTML `<table>`，数据完整 |
| 公式 | LaTeX 较简洁 | LaTeX 空格多，解析成本高 |
| 图表 | 图片路径 `_page_X_*.jpeg` | 含 OCR 描述 + Mermaid 流程图 |
| 参考文献 | 基本完整（有少量排版问题） | 多处卷期页码缺失 |

---

## 三、为什么选 maker 做主数据源

论文预检查 agent 通常需要核对：摘要与正文一致性、方法/实验是否完整、数值是否可追踪、引用是否规范。这些都依赖**正文保真度**，maker 明显更好。

### 1. 关键数值完整

**maker** 摘要中实验指标完整，例如：
- 均方根误差为 **42.29±5.66 pcu**
- WMAPE 为 **4.18%±1.06%**
- 误差分别降低了 **34.20%、36.37%**

**mineru** 同一处大量缺失，例如：
- 均方根误差为 **（空）**
- 6.57%± **（空）**
- 分别降低了 **、**

若用 mineru 做预检查，摘要-正文一致性、结果合理性等规则会直接失效。

### 2. 术语与缩写未丢失

mineru 引言中 ARIMA、SVR、RF、KNN、Mendez、Ren、GRU、TCN 等常被 OCR 成空白；maker 基本保留，便于做术语规范、缩写首次定义等检查。

### 3. 元数据更完整

maker 保留作者单位、邮编、基金、邮箱、DOI、中图分类号等；mineru 作者行常见「西安 ； 海口 」这类地址缺失。

### 4. 章节结构更适合规则/分块

maker 有清晰层级，例如：
- `#### 1 问题描述`
- `#### 2.1 数据预处理`
- `#### 3.3.2 模型预测效果对比`
- `### 参考文献`

mineru 多为 `# 问题描述`、`# 数据预处理`，章节号丢失，不利于按「摘要 / 引言 / 方法 / 实验 / 结论 / 参考文献」分块检查。

---

## 四、mineru 的优势（可作补充）

mineru 并非一无是处，在**结构化抽取**上有价值：

- **HTML 表格**：表 1–4 用 `<table>` 包裹，程序化解析更稳
- **图表语义**：`<details><summary>flowchart</summary>` + Mermaid，对「方法框架是否完整」有帮助
- **图片路径**：`images/*.jpg`，若本地有对应资源，可做多模态检查

但正文大面积缺字，**不能**单独承担预检查主数据源。

---

## 五、maker 的不足（接入时注意）

1. **表格插入位置**：表 1 插在段落中间，分块时需按标题/表格边界切分，不能纯按段落
2. **少量标题 OCR 错误**：如 `#### 关计算公式如下:` 应为 LSTM 小节
3. **参考文献排版**：个别条目跨行、顺序略乱，引用检查前建议做 normalize
4. **图片路径**：`_page_X_*.jpeg` 需确认本地是否存在；若缺失，图表相关检查只能做「引用是否存在」，不能做视觉核对

---

## 六、推荐接入方案

```
主数据源：*_maker.md
  ├─ 全文语义检查（摘要、方法、结论、引用）
  ├─ 数值/术语一致性
  └─ 章节完整性

补充数据源：*_mineru.md（可选）
  ├─ 用 HTML 表格覆盖/校验 maker 表格
  └─ 用 Mermaid/OCR 描述做方法框架图检查
```

---

## 七、后续可扩展方向

- 设计预检查 agent 的分块 schema（按章节 + 表格 + 参考文献）
- 制定具体检查项清单（格式规范、摘要一致性、引用完整性、实验可复现性等）
- 实现 maker 主文本 + mineru 表格/HTML 的融合解析管道
