# pager-api.tyrion.space — 论文纠错后端 API
# DNS: A 记录 pager-api.tyrion.space -> 114.55.139.240
# HTTPS: certbot certonly --nginx -d pager-api.tyrion.space

server {
    listen 80;
    server_name pager-api.tyrion.space;

    client_max_body_size 50m;

    location / {
        proxy_pass http://127.0.0.1:18082;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
        proxy_buffering off;
    }
}

# certbot 申请证书后取消注释，并删除上方纯 HTTP 中的 location /（或改为 301 跳转）
# server {
#     listen 80;
#     server_name pager-api.tyrion.space;
#     return 301 https://$host$request_uri;
# }
#
# server {
#     listen 443 ssl;
#     server_name pager-api.tyrion.space;
#
#     ssl_certificate /etc/letsencrypt/live/pager-api.tyrion.space/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/pager-api.tyrion.space/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     client_max_body_size 50m;
#
#     location / {
#         proxy_pass http://127.0.0.1:18082;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Connection "";
#         proxy_read_timeout 600s;
#         proxy_send_timeout 600s;
#         proxy_buffering off;
#     }
# }
